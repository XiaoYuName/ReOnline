// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace ReDiv.Net.Bindings
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void AwakenCharacterHandler(ReducerEventContext ctx, ulong characterId);
        public event AwakenCharacterHandler? OnAwakenCharacter;

        public void AwakenCharacter(ulong characterId)
        {
            conn.InternalCallReducer(new Reducer.AwakenCharacter(characterId));
        }

        public bool InvokeAwakenCharacter(ReducerEventContext ctx, Reducer.AwakenCharacter args)
        {
            if (OnAwakenCharacter == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnAwakenCharacter(
                ctx,
                args.CharacterId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class AwakenCharacter : Reducer, IReducerArgs
        {
            [DataMember(Name = "character_id")]
            public ulong CharacterId;

            public AwakenCharacter(ulong CharacterId)
            {
                this.CharacterId = CharacterId;
            }

            public AwakenCharacter()
            {
            }

            string IReducerArgs.ReducerName => "awaken_character";
        }
    }
}
