# 检查命令输出规则

适用场景：检查 Git 状态、导入结果、编译结果或文件变更。

1. 必须保留必要的验证，但优先在命令内部汇总结果，避免将逐文件、重复的长输出带回上下文。
2. 优先输出变更数量、状态类型和关键结论。
3. 使用 `git diff --shortstat`、`git status` 后的 `Group-Object` 或其它 shell 汇总方式，避免直接打印整个目录的文件列表。
4. 只有在定位具体问题或用户明确要求明细时，才输出逐文件结果。
